<Window x:Class="INGIN.Helper.HelperView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:INGIN.Helper"
             mc:Ignorable="d"
             Title="О программе" Height="250" Width="200"
             WindowStyle="SingleBorderWindow" ResizeMode="NoResize" 
             Icon="/INGIN;component/Resources/Icons/ingin.ico" WindowStartupLocation="CenterScreen">

    <Grid Margin="0,0,0,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="160" MinHeight="60"/>
            <RowDefinition Height="90"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox Margin="10,10,10,10"
                 Text="{Binding version}"
                 IsReadOnly="True" />

        <Button x:Name="btn_Ok" Content="Ок" HorizontalAlignment="Right" Margin="0,0,10,50" VerticalAlignment="Bottom" Width="100" Height="25" Click="btn_Ok_Click" Grid.Row="1"/>
        <Button x:Name="btn_Bla" Content="Bla" HorizontalAlignment="Right" Margin="0,0,137,50" VerticalAlignment="Bottom" Width="53" Height="24" Click="btn_Bla_Click" Grid.Row="1"/>
    </Grid>
</Window>
